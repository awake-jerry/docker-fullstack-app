#  베이스 이미지를 명시해준다.
FROM node:alpine

#  애플리케이션 소스 코드를 가지고 있을 디렉토리를 명시
#  1.베이스 이미지의 디렉토리 및 파일 덮어쓰기 방지  2.디렉토리 구조 정리
WORKDIR /app

#  로컬에 존재하는 파일을 스냅샷 이미지에 복사
COPY package.json ./

#  DOCKER 이미지가 생성되기 전에 수행될 명령어
#  추가적으로 필요한 파일을 다운로드 or 실행
RUN npm install

#  로컬에 존재하는 파일을 스냅샷 이미지에 복사
COPY ./ ./

#  컨테이너가 시작될때에 실행할 파일 또는 쉘 스크립트를 명시
#  1회만 사용
CMD ["npm", "run", "start"]

